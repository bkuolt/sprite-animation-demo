# API Reference

The internal C++ APIs are fully documented using Doxygen. 

You can generate the HTML documentation locally by running the `docs` CMake target:
```bash
cmake --build build/Release --target docs
```

This will output the documentation to `build/Release/docs/html`, which is styled using `doxygen-awesome-css`.
